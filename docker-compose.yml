version: '2'
services:
    glusterfs:
        image: "glusterfs:8-${CENTOS_ARCH}"
        build:
            context: "./dockerfiles/${CENTOS_ARCH}"
        privileged: true
        container_name: glusterfs
        volumes:
        - ./volumes/data:/data
        - ./volumes/logs:/glusterfs/logs
